import { ReferentAcademique } from './referent-academique';

describe('ReferentAcademique', () => {
  it('should create an instance', () => {
    expect(new ReferentAcademique()).toBeTruthy();
  });
});
