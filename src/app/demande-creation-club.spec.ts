import { DemandeCreationClub } from './demande-creation-club';

describe('DemandeCreationClub', () => {
  it('should create an instance', () => {
    expect(new DemandeCreationClub()).toBeTruthy();
  });
});
